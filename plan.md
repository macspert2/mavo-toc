This project is for the reation of a wordpress plugin which provides a shortcode to create a table-of-contents inside a post based on headings.
The shortcode should provide the common options with sensible defaults (e.g. title, depth or level, fixed/sticky, collapsable, limit/view more etc.
